# save this a simple-job.yaml
apiVersion: batch/v1
kind: Job
metadata:
  name: batch-job-example
spec:
  template:
    spec:
      containers:
        - name: my-job
          image: alpine
          imagePullPolicy: IfNotPresent
          command: ['sh', '-c', 'echo Third Batch Job; sleep 2']
      restartPolicy: Never
      terminationGracePeriodSeconds: 0
  ttlSecondsAfterFinished: 300
